{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Fenix: Build project (cmake)",
            "type": "shell",
            "command": "${workspaceFolder}/build.sh",
            "options": {
                "env": {
                    "CC": "clang",
                    "CXX": "clang++"
                }
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
